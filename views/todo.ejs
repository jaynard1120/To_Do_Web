<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo Page</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"">
</head>
<style>
    body {
    background-color: #f9f9fa;
}.card {
    position: relative;
    display: flex;
    flex-direction: column;
    min-width: 0;
    word-wrap: break-word;
    background-color: #fff;
    background-clip: border-box;
    border: 1px solid #d2d2dc;
    border-radius: 0
}.add-items input[type=" text"] { border-top-right-radius: 0; border-bottom-right-radius: 0; width: 100%; background:
        transparent } .add{ border-top-left-radius: 0; border-bottom-left-radius: 0; width: 15%; } .container{ margin:5%
        auto; width: 60%; }
  
 </style>

<body>
    
    <div class="card-body shadow-lg p-3 mb-5 bg-white rounded p-3 mb-5 card px-3  row container d-flex justify-content-center ">
        <form action="/post" method="post" id="form" autocomplete="off">
            <div class=" add-items d-flex">
                <!-- <input type="hidden" name="id" id="id"> -->
                <input type="text" name="task" class="form-control todo-list-input"
                    placeholder="What do you need to do today?">
                <button type="button" class="add btn btn-primary font-weight-bold todo-list-add-btn">Add</button>
            </div>
        </form>
        <table style="margin-top:3%;">
            <thead>
                <tr>
                    <th>Task</th>
                    <th class="text-center">Created</th>
                    <th style="float: right;">Action</th>
                </tr>
            </thead>
            
            <tbody class="mt-5">
               
                <% for(i = 0; i<data.length ;i++){%>

                    <tr class="mt-5" style="background:#f9f9fa;border-bottom: 1px solid #d2d2dc;">
                        <td class="td"><%=data[i].task%></td>
                        <td class="td text-center"><%=data[i].dateCreated%></td>
                        <td style="float:right"><a class="btn btn-danger font-weight-bold todo-list-add-btn"
                                href="/delete/<%=i%>">delete</a></td>
                    </tr>

                <%}%>
        </tbody>
    </div>
</body>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
    $(document).ready(function () {

        $('.add').click(() => {
            $('#form').submit()
        })
    });
</script>
</html>